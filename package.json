{
  "name": "mock-api-service",
  "version": "1.0.0",
  "description": "Mock server for different entities!!!",
  "main": "index.js",
  "scripts": {
    "start": "node fkData && json-server --watch db/db.json --port 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">= 14.0.0",
    "npm": ">= 6.0.0"
  },
  "author": "amit.kshirsagar.13@gmail.com",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "type": "module",
  "devDependencies": {
    "@faker-js/faker": "^7.3.0"
  }
}
